## RCE专题增强（可插拔模块）

当你完成对 repo 的初步结构理解、但尚未完全锁死到单个漏洞方向时，应考虑调用 `loop9-rce-audit-focus` 作为一次轻量专题预扫。

这一步的目标不是直接证明漏洞，而是：
- 从【RCE / 文件上传 / 弱鉴权推进到执行面】的视角识别高价值功能面
- 初始化或补充 `RCE专题共享上下文.md`
- 给后续主审计提供更值得深挖的方向

注意：
- 这只是增强层，不替代主审计
- 不要把它退化成危险关键词搜索器
- 更应从功能面、权限边界、上传链、模板/渲染链、配置/插件/集成能力去理解项目
