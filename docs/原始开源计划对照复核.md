# LongWangClaw｜原始开源计划对照复核

本复核对照来源：

```text
~/.openclaw/docs_对外产品文档/小龙虾开源计划
```

结论先说：当前仓库已经从“复制一批文件”推进到了“可装配 OpenClaw 工作区”的形态，主链路资产基本齐；但仓库应继续避免把宣发材料、真实报告、target 源码和账号态配置混进来。

## 当前覆盖结论

### 1. 第三个公开整理仓

原始计划要求最终不是直接公开两个本地仓，而是整理成第三个 `LongWangClaw`。

当前覆盖：

- `README.md`
- `docs/12-开源范围与仓库关系说明.md`
- `scripts/bootstrap_openclaw_layout.py`

当前判断：已覆盖。仓库按 `workspace/`、`extensions/`、`templates/`、`scripts/` 组织，不改成 `src/` 包。

### 2. OpenClaw 作为 AI OS / 控制面底座

原始计划强调 OpenClaw 是底座，LongWangClaw 是其上的安全工作流产品层。

当前覆盖：

- `docs/30-OpenClaw在龙王小龙虾里的角色说明.md`
- `workspace/OpenClaw_实装入口设计.md`
- `workspace/TELEGRAM_小龙虾封装说明.md`
- `extensions/openclaw-lark/`
- `workspace/bin/`
- `workspace/lib/`
- `workspace/hooks/`
- `workspace/plugins/`

当前判断：已覆盖。公开仓保留控制面工作区资产和飞书插件，不复制真实 `~/.openclaw/openclaw.json`。

### 3. Super8 是 Audit / Real-PoC / Loop9 主线核心

原始计划把 Super8 定位为 audit、real-poc 和 Loop9 原始工作流的核心仓。

当前覆盖：

- `workspace/Super8/.opencode/command/loop9.md`
- `workspace/Super8/.opencode/agents/`
- `workspace/Super8/.opencode/_scripts/loop9_authorized_review.py`
- `workspace/Super8/.opencode/_scripts/loop9_status.sh`
- `workspace/Super8/.opencode/_xml/loop9.master.xml`
- `workspace/Super8/工作流_提示词工程/`
- `workspace/skills/loop9-wrapped-audit/`
- `workspace/skills/loop9-real-poc/`

当前判断：已覆盖。wrapper、agents、command 和两个 prompt 模板都在公开闭包里。

### 4. 端到端闭环

原始计划里的真实链路是目标发现、环境搭建、自动化验证、PoC 复现、Real-PoC、证据沉淀、报告交付。

当前公开仓按六段对外运行链路收口：

1. Audit：`workspace/skills/loop9-wrapped-audit/SKILL.md`
2. Real-PoC 静态编写：`workspace/skills/loop9-real-poc/SKILL.md`
3. 静态 Audit/PoC 产物上传：`workspace/skills/loop9-feishu-publisher/SKILL.md`
4. 动态环境搭建与 PoC 复现：`workspace/skills/loop9-verify-v4/SKILL.md`
5. 本地标准交付报告：`workspace/skills/loop9-delivery-reports/SKILL.md`
6. 动态复现后的标准报告上传：`workspace/skills/loop9-feishu-delivery-publisher/SKILL.md`

当前判断：核心链路已覆盖。两段上传已经拆开，避免把 raw artifacts 发布和标准报告发布混成一个动作。

### 5. Dispatcher 主线

原始计划强调 Dispatcher 是主线 owner，不是普通 wrapper。

当前覆盖：

- `docs/31-Dispatcher主线说明.md`
- `workspace/bin/loop9-dispatch`
- `workspace/bin/loop9-launch-guard`
- `workspace/lib/loop9_automation/`
- `workspace/plans/2026-03-27-loop9-dispatcher-source-of-truth.md`

当前判断：已覆盖，并已修正公开文档里的旧阶段名。当前 active 动态复现入口是 `loop9-verify-v4` family，而不是旧 `loop9-env-bootstrap` / `loop9-poc-verification`。

### 6. Skill / Tool / Wrapper / Harness 分层

原始计划要求讲清 Skill、Tool、Wrapper、Harness 边界。

当前覆盖：

- `docs/33-Skill_Tool_Wrapper_Harness工程说明.md`
- `docs/34-AI原生工作流哲学说明.md`
- `harness/README.md`
- `harness/loop9-verify-v4-control-contract.md`
- `workspace/skills/ai-native-development/SKILL.md`
- `workspace/skills/*/SKILL.md`
- `workspace/Super8/.opencode/_scripts/loop9_authorized_review.py`

当前判断：已按你的拍板单独开 `harness/`。这个目录只放 active 控制面纪律说明，不搬历史报告、运行产物或调试原型。

### 7. 模型能力与统一配置

原始计划要求说明模型能力门槛，并让用户能替换本机模型/工具配置。

当前覆盖：

- `docs/22-模型能力要求说明.md`
- `workspace/config/longwang.example.json`
- `templates/openclaw/openclaw.json.tpl`
- `templates/opencode/opencode.json.tpl`
- `templates/codex/config.toml.tpl`
- `templates/cron/jobs.json.tpl`
- `templates/env/longwang.env.tpl`
- `scripts/render_longwang_config.py`

当前判断：已覆盖，并已补强。渲染器现在还会把 OpenCode agents 的 `model` / `variant` frontmatter 写成 profile 里的值，避免用户改了全局模型但 agents 仍引用公开默认值。runtime 配置也区分 local Docker 与远程 Docker/CVM。

### 8. Cron / 长任务推进

原始计划把 cron、heartbeat、长任务调度列为关键机制。

当前覆盖：

- `workspace/plans/loop9-hourly-cron-jobs.example.json`
- `workspace/plans/loop9_cron/`
- `templates/cron/jobs.json.tpl`
- `workspace/heartbeat/loop9-status-dispatch.md`
- `workspace/bin/loop9-cron-install`

当前判断：已覆盖。公开边界是五条 lane，不引入 CI / smoke / demo。

### 9. Feishu / Lark 发布

当前目标需要两段上传：静态 PoC/审计产物上传，以及动态复现后的标准报告上传。

当前覆盖：

- `extensions/openclaw-lark/`
- `workspace/skills/loop9-feishu-publisher/`
- `workspace/skills/loop9-feishu-delivery-publisher/`
- `docs/端到端运行手册.md`
- `docs/配置与私有化指南.md`

当前判断：已覆盖。真实 Feishu appId/appSecret、账号和 OAuth 态只由本机 profile / OpenClaw 配置承担，不入仓。

### 10. 运行报告、案例和 targets

原始产品计划中报告和案例可用于宣发，但你已经明确要求开源仓不带报告、不带靶标源码、不做 demo。

当前覆盖：

- `.gitignore`
- `docs/12-开源范围与仓库关系说明.md`
- `docs/46-关键源码路径索引与片段引用.md`
- `scripts/longwang_doctor.py`
- `fixtures/contract/`

当前判断：边界正确。`reports/`、`runs/`、`targets/`、`memory/`、`logs/` 都不进入公开仓。`fixtures/contract/` 只作为 contract-only 合成夹具保留，不是 demo target、真实案例或真实报告。

## 本轮已修正的缺口

- `docs/12-开源范围与仓库关系说明.md`：从“筹备骨架稿”改成当前公开边界。
- `docs/31-Dispatcher主线说明.md`：去掉旧 active 阶段名，改成 `loop9-verify-v4` family。
- `docs/46-关键源码路径索引与片段引用.md`：从“本机路径/宣发引用索引”改成当前公开仓路径索引。
- `scripts/render_longwang_config.py`：新增 OpenCode agents frontmatter 渲染。
- `workspace/skills/loop9-wrapped-audit/SKILL.md`：metadata 从 Darwin 扩到 Darwin/Linux。
- `harness/`：按你的拍板新增独立 Harness 高级工程目录。
- `fixtures/contract/`：新增最小合成契约夹具，用于 doctor 和发布计划解析，不承载真实漏洞或首发案例。

## 已对齐的产品取舍

### A. 宣发/运营类文档不入仓

原始目录里有 `10-对外一页纸.md`、`11-产品定位与能力边界.md`、`41-运营宣发_briefing.md`、`42-可说_慎说_禁说清单.md`、`43-标题池与对标切入角度池.md`、`44-FAQ_面向外部与运营.md`、`45-首发案例名单.md`、`47-演示与直播复用材料整理.md`。

已按你的最新拍板处理：宣发/运营类文档不加入当前对外开源仓。

这些材料只作为宣发侧后续人工选材来源，不作为当前运行仓资产。

### B. Harness 单独成目录

已按你的最新拍板处理：新增 `harness/`。

当前只放：

- `harness/README.md`
- `harness/loop9-verify-v4-control-contract.md`

不放 live validation 报告、历史运行产物、target 源码、真实 demo、CI 或 smoke。

### C. 首发案例名单不公开入仓

已按你的最新拍板处理：不公开首发案例名单。

首发案例只作为宣发时人工选取的材料，由宣发侧决定是否采用，不进入当前 GitHub 开源仓。

## 当前一句话判断

当前仓库已经更接近原始目标：别人拿到后，填 `longwang.local.json`，跑渲染和 doctor，就能知道 Audit、Real-PoC、静态上传、动态复现、报告生成、动态报告上传和 cron 哪些已经可跑、哪些还差本机配置。没有授权目标时，contract fixture 可以证明公开仓的最小契约可解析；真实能力仍必须用用户自有授权目标首跑验证。
